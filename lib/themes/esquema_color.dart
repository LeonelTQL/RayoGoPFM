import 'package:flutter/material.dart';

class EsquemaColor {
  // Nueva paleta Premium "RayoGo" - Estilo Delivery Profesional
  static const primary = Color(0xFFFF5722); 
  static const primaryDark = Color(0xFFE64A19);
  static const accent = Color(0xFF2D3436); 
  
  static const background = Color(0xFFF8F9FA);
  static const surface = Color(0xFFFFFFFF); 
  static const card = Color(0xFFFFFFFF);
  
  // Tipografía
  static const textPrimary = Color(0xFF2D3436);
  static const textSecondary = Color(0xFF636E72);
  static const muted = Color(0xFFB2BEC3);
  
  // Colores para Banners/Chips (Ajustados para legibilidad)
  static const chip = Color(0xFFF1F2F6); // Gris muy claro para que el texto negro se vea
  static const line = Color(0xFFECF0F1);
  
  // Estados (Más brillantes para resaltar)
  static const success = Color(0xFF00B894); 
  static const warning = Color(0xFFFDCB6E);
  static const danger = Color(0xFFD63031);

  static const infoBanner = Color(0xFFE1F5FE); // Azul claro para info
  static const infoText = Color(0xFF01579B);

  // Compatibilidad
  static const dark = Color(0xFF2D3436);
  static const purple = Color(0xFFFF5722);
}
